/**
 * Automatically generated by @grafana/openapi-to-k6: 0.4.1
 * Do not edit manually.
 * Link Example
 * Service version: 1.0.0
 */
import { URL, URLSearchParams } from "https://jslib.k6.io/url/1.0.0/index.js";

import http from "k6/http";
import type { Params, Response } from "k6/http";

export interface User {
  username?: string;
  uuid?: string;
}

export interface Repository {
  slug?: string;
  owner?: User;
}

export interface Pullrequest {
  id?: number;
  title?: string;
  repository?: Repository;
  author?: User;
}

export type GetPullRequestsByRepositoryParams = {
  state?: GetPullRequestsByRepositoryState;
};

export type GetPullRequestsByRepositoryState =
  (typeof GetPullRequestsByRepositoryState)[keyof typeof GetPullRequestsByRepositoryState];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetPullRequestsByRepositoryState = {
  open: "open",
  merged: "merged",
  declined: "declined",
} as const;

/**
 * This is the base client to use for interacting with the API.
 */
export class LinkExampleClient {
  private cleanBaseUrl: string;
  private commonRequestParameters: Params;

  constructor(clientOptions: {
    baseUrl: string;
    commonRequestParameters?: Params;
  }) {
    this.cleanBaseUrl = clientOptions.baseUrl.replace(/\/+$/, "");

    this.commonRequestParameters = clientOptions.commonRequestParameters || {};
  }

  getUserByName(
    username: string,
    requestParameters?: Params,
  ): {
    response: Response;
    data: User;
    operationId: string;
  } {
    const k6url = new URL(this.cleanBaseUrl + `/2.0/users/${username}`);
    const mergedRequestParameters = this._mergeRequestParameters(
      requestParameters || {},
      this.commonRequestParameters,
    );
    const response = http.request(
      "GET",
      k6url.toString(),
      undefined,
      mergedRequestParameters,
    );
    let data;

    try {
      data = response.json();
    } catch {
      data = response.body;
    }
    return {
      response,
      data,
      operationId: "getUserByName",
    };
  }

  getRepositoriesByOwner(
    username: string,
    requestParameters?: Params,
  ): {
    response: Response;
    data: Repository[];
    operationId: string;
  } {
    const k6url = new URL(this.cleanBaseUrl + `/2.0/repositories/${username}`);
    const mergedRequestParameters = this._mergeRequestParameters(
      requestParameters || {},
      this.commonRequestParameters,
    );
    const response = http.request(
      "GET",
      k6url.toString(),
      undefined,
      mergedRequestParameters,
    );
    let data;

    try {
      data = response.json();
    } catch {
      data = response.body;
    }
    return {
      response,
      data,
      operationId: "getRepositoriesByOwner",
    };
  }

  getRepository(
    username: string,
    slug: string,
    requestParameters?: Params,
  ): {
    response: Response;
    data: Repository;
    operationId: string;
  } {
    const k6url = new URL(
      this.cleanBaseUrl + `/2.0/repositories/${username}/${slug}`,
    );
    const mergedRequestParameters = this._mergeRequestParameters(
      requestParameters || {},
      this.commonRequestParameters,
    );
    const response = http.request(
      "GET",
      k6url.toString(),
      undefined,
      mergedRequestParameters,
    );
    let data;

    try {
      data = response.json();
    } catch {
      data = response.body;
    }
    return {
      response,
      data,
      operationId: "getRepository",
    };
  }

  getPullRequestsByRepository(
    username: string,
    slug: string,
    params?: GetPullRequestsByRepositoryParams,
    requestParameters?: Params,
  ): {
    response: Response;
    data: Pullrequest[];
    operationId: string;
  } {
    const k6url = new URL(
      this.cleanBaseUrl +
        `/2.0/repositories/${username}/${slug}/pullrequests` +
        `?${new URLSearchParams(params).toString()}`,
    );
    const mergedRequestParameters = this._mergeRequestParameters(
      requestParameters || {},
      this.commonRequestParameters,
    );
    const response = http.request("GET", k6url.toString(), undefined, {
      ...mergedRequestParameters,
    });
    let data;

    try {
      data = response.json();
    } catch {
      data = response.body;
    }
    return {
      response,
      data,
      operationId: "getPullRequestsByRepository",
    };
  }

  getPullRequestsById(
    username: string,
    slug: string,
    pid: string,
    requestParameters?: Params,
  ): {
    response: Response;
    data: Pullrequest;
    operationId: string;
  } {
    const k6url = new URL(
      this.cleanBaseUrl +
        `/2.0/repositories/${username}/${slug}/pullrequests/${pid}`,
    );
    const mergedRequestParameters = this._mergeRequestParameters(
      requestParameters || {},
      this.commonRequestParameters,
    );
    const response = http.request(
      "GET",
      k6url.toString(),
      undefined,
      mergedRequestParameters,
    );
    let data;

    try {
      data = response.json();
    } catch {
      data = response.body;
    }
    return {
      response,
      data,
      operationId: "getPullRequestsById",
    };
  }

  mergePullRequest(
    username: string,
    slug: string,
    pid: string,
    requestParameters?: Params,
  ): {
    response: Response;
    data: void;
    operationId: string;
  } {
    const k6url = new URL(
      this.cleanBaseUrl +
        `/2.0/repositories/${username}/${slug}/pullrequests/${pid}/merge`,
    );
    const mergedRequestParameters = this._mergeRequestParameters(
      requestParameters || {},
      this.commonRequestParameters,
    );
    const response = http.request(
      "POST",
      k6url.toString(),
      undefined,
      mergedRequestParameters,
    );
    let data;

    try {
      data = response.json();
    } catch {
      data = response.body;
    }
    return {
      response,
      data,
      operationId: "mergePullRequest",
    };
  }

  /**
   * Merges the provided request parameters with default parameters for the client.
   *
   * @param {Params} requestParameters - The parameters provided specifically for the request
   * @param {Params} commonRequestParameters - Common parameters for all requests
   * @returns {Params} - The merged parameters
   */
  private _mergeRequestParameters(
    requestParameters?: Params,
    commonRequestParameters?: Params,
  ): Params {
    return {
      ...commonRequestParameters, // Default to common parameters
      ...requestParameters, // Override with request-specific parameters
      headers: {
        ...(commonRequestParameters?.headers || {}), // Ensure headers are defined
        ...(requestParameters?.headers || {}),
      },
      cookies: {
        ...(commonRequestParameters?.cookies || {}), // Ensure cookies are defined
        ...(requestParameters?.cookies || {}),
      },
      tags: {
        ...(commonRequestParameters?.tags || {}), // Ensure tags are defined
        ...(requestParameters?.tags || {}),
      },
    };
  }
}
